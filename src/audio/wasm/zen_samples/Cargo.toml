[package]
name = "zen_samples"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
wasm-bindgen = "0.2"

# Avoid wasm-opt transforms that Webpack 5's @webassemblyjs parser cannot decode (see webpack build).
[package.metadata.wasm-pack.profile.release]
wasm-opt = false
